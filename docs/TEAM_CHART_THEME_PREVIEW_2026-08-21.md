# Team Chart Theme Preview

The live Los Angeles Dodgers Performance workspace confirms that the selected club accent is already applied to the club-owned Offensive Profile radar, Team Strengths radar, Run Differential chart, Exit Velocity Distribution, and historical payroll line. The new preview extends that contract to the selected row in Divisional WAR Comparison: when verified FanGraphs rows are available, the selected club will use its readable team accent and peer teams will retain neutral purple comparison bars.

The current live view has no verified divisional WAR rows, so no team-colored comparison bar is rendered. This preserves the no-fabrication policy. The panel explicitly remains unavailable until verified rows are returned. The preview was reviewed at desktop and mobile widths; selected-club blue remains readable, and the display-safe Padres brown is defined as `#78502D` for small text and chart accents.
