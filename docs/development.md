# Development Guide

## Quick Start

```bash
pnpm install --frozen-lockfile
pnpm run check
pnpm run lint
pnpm run test
pnpm run build
pnpm run dev
```

## Conventions

- Preserve data provenance badges on all metric displays.
- Never substitute synthetic data for missing stats.
- Run type checking and tests before saving checkpoints.
